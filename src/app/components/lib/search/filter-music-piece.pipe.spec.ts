import { FilterMusicPiecePipe } from './filter-music-piece.pipe';

describe('FilterMusicPiecePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMusicPiecePipe();
    expect(pipe).toBeTruthy();
  });
});
