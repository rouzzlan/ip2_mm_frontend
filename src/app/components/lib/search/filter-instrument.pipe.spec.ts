import { FilterInstrumentPipe } from './filter-instrument.pipe';

describe('FilterInstrumentPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterInstrumentPipe();
    expect(pipe).toBeTruthy();
  });
});
