import { FilterInstrumentsPipe } from './filter-instruments.pipe';

describe('FilterInstrumentsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterInstrumentsPipe();
    expect(pipe).toBeTruthy();
  });
});
